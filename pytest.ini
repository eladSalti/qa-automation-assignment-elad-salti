[pytest]
minversion = 8.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-config
    --strict-markers
    --browser chromium
markers =
    ui: Swag Labs browser tests
    api: JSONPlaceholder API tests
